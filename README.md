# Practice Repo

A minimal Python project for practicing git workflows and AI tooling.

## Run tests (one command)

```bash
python -m pytest test_calculator.py -v
```

Or without pytest:

```bash
python -m unittest test_calculator.py -v
```
